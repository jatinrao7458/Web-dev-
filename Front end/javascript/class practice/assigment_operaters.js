let a = 3
let b = "3"
console.log(a==b)
// == means that check if the value is same (but type can be any )then console true and false (type can be same or diff )accordingly
console.log(a===b)
//  === means check type and print true and false (value can be same or diff)
console.log(a!=b)
// != this means that the value is not same ( type can be same or diff)
console.log(a!==b)
// !== this means that not the type  are diff(value can be same or not)